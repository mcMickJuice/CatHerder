import React from 'react';


const App = () => <div>Hello World hi!</div>;

// check to see if user is logged in

// if logged in, continue to app, injecting in user

// if not, route to login "app"

export default App;
